KAIROS Architecture (v0.1)
textKAIROS
├── Knowledge Layer          ← Obsidian Vault + Markdown + Git
├── AI Team Layer            ← config/agents.yaml
├── Project Layer            ← Aether Crew Lite (Obsidian)
├── Asset Layer              ← KAIROS_ASSET_LIBRARY (local / Google Drive / NAS / S3)
├── Approval Layer           ← APPROVAL_QUEUE.md + Permission Matrix
├── Automation Layer         ← n8n (v0.3)
└── Interface Layer          ← PowerShell + Obsidian + Future GUI
핵심 원칙

Markdown is the source of truth
AI tools are workers, not owners
Local-first (Cloud is backup only)
